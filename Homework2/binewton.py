def binewton(x0, x1):
    newtonmethod(x0)
    
    i = 0
    while i < 4:
        xr = (x0+x1)/2
        fx = function (xr)
        fx1 = function (x1)
        if (fx * fb) < 0:
            x0 = x
        else:
            x1 = x
        i = i + 1
  newtonmethod(
